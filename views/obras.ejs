<%- include('partials/obrasHead') %>
<body>
    <%- include('partials/header') %>

    
    <section id="obras">
      <div id="particles-js"></div>
      <div class="logo__title" data-aos="zoom-out">
        <img src="css/svg/logo-admin.svg" />
  
          <!-- / TITULO / -->
          <h1><a href="/" class="title">STRUMEL</a></h1>
        </div>

        <div class="obras__grid">
            <%obras.forEach((obra,i) => { %>   <!-- %%%%%%% OBRA.FOREACH -->
                <div class="obra__box" data-aos="fade-up">
                        <img data-id="<%= obra.id %>" class="gallery__img" src="/css/images/loader.gif" alt="<%=obra.titulo%>">
                        <span class="toggle-overlay"></span>
                        <div class="obra__overlay">
                            <div class="obra__overlay--inner">
                                <% let resumen = obra.resumen %>
                                <%if(resumen.length > 60){resumen = obra.resumen.substring(0,60) + "..."}%>   

                                <h1><%=obra.titulo%></h1>
                                <h2><%=resumen%></h2>
                                <button class="btn--animated info-btn" name="<%= i %>">Saber Más</button>
                                
                            </div>
                        </div>
                </div>
            <%})%>
                <!-- %%% OBRA OVERLAY %%% -->
        <% obras.forEach((obra, i)=> { %>
            <div class="obra-info">
              <div class="obra-info--inner">
  
                <button class="obra-info--close" name=<%= i %>>
                  <i class="fas fa-times"></i>
                </button>
                <span class="obra-info__date"><%= obra.fecha.toLocaleDateString("de-DE") %></span>
               <div class="obra-info__img">
                 <img data-id="<%= obra.id %>" src="/css/images/loader.gif" alt="<%= obra.resumen %>">
                 <button class="obra-info__expand-img-btn"><i class="fas fa-expand"></i></button>
                </div>
                <div class="contenido-scroll">
               <h3><%= obra.resumen %></h3>
               
                 <p><%= obra.contenido %></p>
               </div>
  
             </div>
            </div>
  
          <% }) %>
        </div>
    </section>
    <script src="js/particles/particles.min.js"></script>
    <script src="js/particles/particlesConfig.js"></script>
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script async src="/js/mobileImageZoom.js"></script>
    <script src="/js/obraScript.js"></script>
</body>
</html>